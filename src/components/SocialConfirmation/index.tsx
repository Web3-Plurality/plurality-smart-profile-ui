import './styles.css'

const SocialConfirmation = ({ selectedSocial }: { selectedSocial: string }) => {
    return (
        <div>
            {selectedSocial}
        </div>
    )
}

export default SocialConfirmation
